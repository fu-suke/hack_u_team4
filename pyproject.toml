[project]
name = "linux-virus"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "honcho>=2.0.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]
