[project]
name = "linux-virus"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "linux-virus-frontend",
]

[tool.uv]
package = false

[tool.uv.sources]
linux-virus-frontend = { workspace = true }

[tool.uv.workspace]
members = [
    "frontend",
]
